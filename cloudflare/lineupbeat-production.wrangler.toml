name = "lineupbeat"
pages_build_output_dir = "site"
compatibility_date = "2026-09-04"

[[d1_databases]]
binding = "LEAGUE_HISTORY_DB"
database_name = "lineupbeat-league-history-production"
# The production workflow resolves or creates this database, verifies that it
# is not the development database, and replaces this placeholder before deploy.
database_id = "__PRODUCTION_LEAGUE_HISTORY_DB_ID__"
